extends layout

block content
    h1 #{title}: #{bookinstance._id}
    // FIXME: bookinstance doesn't show name(might need to get book in controller)
    p #[strong Title:]
        a(href=bookinstance.book.url) #{bookinstance.book.title}
    p #[strong Imprint:] #{bookinstance.imprint}

    p #[strong Status:]
        if bookinstance.status=='Available'
            span.text-success #{bookinstance.status}
        else if bookinstance.status=='Maintenance'
            span.text-danger #{bookinstance.status}
        else
            span.text-warning #{bookinstance.status}
    p Do you really want to delete this Copy?

    form(method='POST' action='')
        div.form-group
        input#bookinstanceId.form-control(type='hidden',name='bookinstanceId', required='true', value=bookinstance._id )

        button.btn.btn-primary(type='submit') Delete
